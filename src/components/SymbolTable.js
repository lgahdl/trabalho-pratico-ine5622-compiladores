const SymbolTable = ({ symbolTableJSON, className }) => {
  return (
    <div className={`${className}   `} style={{ height: "400px", width: "100%", backgroundColor: "red" }}>
    
    </div>
  )
}

export default SymbolTable